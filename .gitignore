# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# PlatformIO build & local configuration files
.pio/
.vscode/.provider
firmware/include/config.h

# Environment files
.env
.env.local
.env.*.local

# VS Code Local History extension
.history/

# Wrangler CLI local state
.wrangler/

# Docs & AI agent artifacts — DEV BRANCH ONLY.
# main/staging intentionally do not carry docs/, planning, or prompt
# files; dev is the canonical home for them. Merging an untracked file
# from dev back into main requires re-running "git rm -r --cached <file>".
docs/
*.prompt.md

# AI agent working directories (kept out of deployment branches)
.opencode/
.claude/
.cursor/
.agents/
